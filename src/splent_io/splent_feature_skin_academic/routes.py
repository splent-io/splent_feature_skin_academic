# skin_academic exposes no page routes — it only serves its stylesheet (via the
# blueprint asset route) and injects it through the layout.head hook.
from splent_io.splent_feature_skin_academic import skin_academic_bp  # noqa: F401
