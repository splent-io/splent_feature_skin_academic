"""skin_academic ships its aesthetic stylesheet through the asset registry
(declared in __init__.init_feature with order 200, so it cascades last over the
theme's base public.css). No template hooks are needed.
"""
